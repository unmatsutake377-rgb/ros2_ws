# 소프트파트 작업 체크리스트

> 기준: 2026-08-06 · 담당: 소프트(노드/로직) · 회로/펌웨어는 `docs/문제와작업/회로펌웨어_현황판.html`
> `[x]` 완료 / `[ ]` 대기 / `⏸` 실측·외부답 대기
> ⚠️ 8월이 폐루프 마감 창.

---

## 🔴 지금 바로 (막힌 것 — 최우선)

- [ ] **push** — 미push 3커밋(healthcheck 로거fix·navpvt/sea·병합). Ubuntu 실기 재개하려면 필요
- [ ] **팀원 회신 전달** — `docs/전달용/`에 회신 8건 완성, **아직 안 보냄. 3연속 최대 병목.**
      GPS LOS 이식·IMU driver diff가 이 답 대기 → **오늘 보내면 답 오는 동안 병렬 진행**

## 🟢 Ubuntu 이어서 (실물 필요, push 후)

- [ ] **남은 실기 시나리오** — 로거 버그 고쳐졌으니 재개: 시나리오1(LiDAR 복구) 재확인 + IMU 끊김·워치독·자동복구
- [ ] **GPS 실외 fix** — F9P 밖에서 위성 잡기(실내 fix 없었음)
- [ ] **udev** — F9P→`/dev/ttyGPS`(config가 이 이름 씀), LiDAR/IMU/Mega 포트도
- [ ] **NTRIP 새 계정 발급 + config** — 작년 것 폐기. `zed_f9p_rover.yaml` RTCM3 경로에. RTK 붙이면 sea 모델 실동작 확인 (GitHub 올리지 말 것, 로컬만)

## 🟡 이식/반영 대기 (신호 오면)

- [ ] **GPS LOS 이식** — 팀원 `los_logic`/`gps_filter` 좋음(점프필터·DR제거). LOS 안정 신호 → 우리 yaml+geofence 최신본에 한 번에 이식(안 A). **회신 전달이 선행**
- [ ] **IMU driver B** — mag_heading A안(융합) 넣음(b433662). 진짜 독립 raw-mag 원하면 데이터시트 mag 명령만
- [ ] **2025 브리지 되살리기** — 회로AI 채택. 포트 ACM1→udev·패키징·상태피드백

## ⏸ 배 뜬 뒤 실측 (측정으로 결정)

- ⏸ **장착각·조향 부호** — 배 뜨는 날 제일 먼저(🚨)
- ⏸ 진동 → `rate_limit_max_deg_per_tick`(15) 튜닝 (blackbox desired_angle/pwm/imu_yaw)
- ⏸ RTK 끊김 빈도 → 심하면 DR/보완필터 재론(EKF 안 함 확정)
- ⏸ 색·거리 정확도 + HSV 대회장 재튜닝 · median 커널(5→7/9) · 테스트배 motor 파라미터
- ⏸ CPU 실부하 재측정 — 경부하 77°C, 카메라 붙으면 스로틀 가능(boat_boot.sh performance 거버너 감안)

## ✅ 완료 (실기 세션 포함)

- [x] Ubuntu 노트북 세팅 + `colcon build` 실빌드 검증
- [x] **로거 심각도 버그 2곳 fix** — ship_direction(aabb3bc, 실물검증) + healthcheck(b9a521b, Mac스윕 발견)
- [x] clone-직후 빌드 실패 3건(color_shape_detector·ntrip_client·iahrs CMakeLists)
- [x] **F9P(B배) rover config 신설 + launch 배별 인자화**(c1b0d81) — A/B 둘 다 지원
- [x] **navpvt 토픽 불일치 수정**(/ublox→/ublox_gps_node) + dynamic_model sea(ed2a144)
- [x] GPS 기본 = B배(F9P) 확정 — "A배 기본" 철회, c1b0d81 유지
- [x] ship_direction v2.2.2(후진 상태기계+rate limiter) — **실물 검증**(후진0.5s→STOP3s·motor_reverse 동기)
- [x] blackbox 센서지연/카메라/mag_heading 컬럼 · iahrs mag_heading 발행
- [x] waypoint_recorder(현장 GPS 기록) · 장애물 필터 단위테스트 · 팀원 제안 8건 회신

## 확정 결정 (되돌리지 않음)

- EKF 미적용 · GPS 기본 B배(F9P) · 우천모드 통째 채택 X · 브리지 발굴본 되살리기 · 수조/실기 테스트=새 코드 X 절차 · c1b0d81 유지

## ✅ 회로팀 확정(현황판)

RC↔자동 전환(CH스위치) · 스러스터 좌우페어(차동 됨) · 브리지 방향(2025 되살리기). 남음: Mega .ino·핀배정·전원

---

## 한눈에 — 다음 순서

| 시점 | 할 것 |
|---|---|
| **바로** | ① push ② **회신 8건 전달**(최대 병목) |
| **push 후(Ubuntu)** | 남은 시나리오 · GPS 실외fix · udev · NTRIP |
| **답 오면** | GPS LOS 이식 · IMU driver B · 브리지 |
| **배 뜬 뒤** | 장착각·조향부호 → 진동·RTK·색거리·모터·CPU 실측 |
