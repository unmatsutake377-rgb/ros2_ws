# 소프트파트 작업 체크리스트

> 기준: 2026-07-30 · 담당: 소프트(노드/로직) · 회로/펌웨어는 `docs/문제와작업/회로펌웨어_현황판.html`
> `[x]` 완료 / `[ ]` 대기 / `⏸` 실측·외부답 대기

---

## ✅ 오늘(07-30) 한 것

**코드 반영:**
- [x] `ship_direction` v2.2.2 이식 — 후진 재탐색 상태기계[L] + 조향 rate limiter[K](0~160° 한정). 우리 base, 회귀 0, failsafe 27/27
- [x] `blackbox` N4 `imu_mag_heading` 컬럼 추가(관찰용)
- [x] `waypoint_recorder` 신규 — 현장 RC 주행 중 GPS 자동 기록(평균·검증·새파일) + 편집(l/d/r)
- [x] (앞서) `blackbox` 센서 도착지연 + 카메라 결과값 컬럼

**팀원 코드 검토 회신(문서):** N4 mag_heading · LOS강화/IMU추가 · 우천모드 v2 · 후진/180 rate · GPS 기능추출 · 2025 브리지

**협업/기타:** 회로팀 3커밋 병합(브리지 되살리기 채택·Mega·WS2812) / 시스템 설명서 수조테스트·GPS recorder 최신화 / 노트북 초기화 완료

---

## 🟢 지금 우리가 할 수 있는 것

- [ ] **Ubuntu 노트북 세팅** — 초기화 완료. 순서: 기본도구→Node→**Claude 설치**→(Ubuntu Claude가) clone+ROS2 Humble+`colcon build`
  - colcon build 시 **한 번에 검증**: waypoint_recorder(roundtrip)·blackbox 새 컬럼·ship_direction 2.2.2·waypoints.yaml 파싱
- [ ] **NTRIP 새 계정 발급** (작년 것 폐기)
- [ ] 팀원 회신 전달 (회신 6건 `docs/전달용/`)
- [ ] udev rules (장비 오면 — LiDAR/IMU/GPS/Mega 포트 고정)

## 🟡 이식/반영 대기 (신호 오면)

- [ ] **GPS LOS 이식** — 팀원 `los_logic`/`gps_filter` 모듈 좋음(점프필터 復活·DR 제거). **LOS 안정 신호 오면** 우리 yaml+geofence 최신본에 한 번에 이식(안 A). waypoints.yaml·cov 길이가드·GPS stale 경고·테스트는 우리가 추가
- [ ] **IMU driver /imu/mag_heading** — blackbox 컬럼은 이식 완료. **팀원이 A/B 확정 + 우리 드라이버 base 실제 diff를 브랜치로** 줘야 반영(보낸 파일은 다른 드라이버였음)
- [ ] **2025 브리지 되살리기** — 회로AI 채택함. 포트 ACM1→udev · 패키징(setup.py/pyserial/launch) · 상태피드백 읽기 반영 대기

## ⏸ 배 실측 후 (측정으로 결정)

- ⏸ **진동** → `rate_limit_max_deg_per_tick`(현 15) 튜닝. blackbox `desired_angle`/`pwm`/`imu_yaw`
- ⏸ **RTK 끊김 빈도** → 심하면 DR/보완필터 재론(EKF는 안 함 확정)
- ⏸ 센서 도착지연(`imu_dt_max`/`gps_dt_max`/`cam_dt_max`)
- ⏸ 색·거리 정확도 + HSV 대회장 재튜닝
- ⏸ median 커널(5→7/9) · 테스트배 motor 파라미터 · ship_goal 0.5초 주기
- ⏸ 스러스터 L/R 물리 배선 벤치 확인

## 🔴 외부 답 대기 (회로/기구팀)

- [x] RC↔자동 전환 = 조종기 CH 스위치 (현황판)
- [x] 스러스터 4개 = 선수2+선미2 **좌/우 페어 → 차동 회전 됨** (현황판, L/R 계약 유효)
- [x] 브리지 방향 = 2025 되살리기 (회로AI 채택)
- ⏸ Mega `.ino` 실존(Q①) / ESC 모델(Q②) / 수신기(Q③) / 핀배정표(Q④) / 전원 회의(Q⑤)

---

## 확정 결정 (되돌리지 않음)

- EKF 미적용 · 우천모드 통째 채택 X(①lidar_noise 발행·②클러스터·④프레임 미해결) · 브리지 발굴본 되살리기 · 수조 테스트=새 코드 X 절차

## 한눈에 — 다음 순서

| 시점 | 할 것 |
|---|---|
| **바로** | Ubuntu에 Claude 설치 → ROS2 세팅 → colcon build 일괄 검증 · NTRIP · 회신 전달 |
| **신호 오면** | GPS LOS 이식(안정 시) · IMU driver diff(브랜치) · 브리지 되살리기 |
| **배 뜬 뒤** | 진동(rate tick)·지연·RTK끊김·색거리·모터튜닝 실측 |
| **대회장** | GPS 좌표 recorder로 찍기 · HSV 재튜닝 · 최종 벤치 |
