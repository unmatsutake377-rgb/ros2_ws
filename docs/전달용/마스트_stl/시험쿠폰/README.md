# 시험 프린트 쿠폰 (v4 기준, 2026-09-16)

절차·합격기준: `docs/절차/마스트_시험프린트.md`
생성: `python3 tools/cad/mast.py` → `python3 tools/cad/fit_test.py`

| 파일 | 크기(mm) | 우선순위 |
|---|---|---|
| `fit_cradle_d455.stl` | 133 × 37 × 33.5 | 1 — 실물 D455 로 바로 확인 |
| `fit_pocket_imu.stl` | 92 × 68 × 20 | 2 — 실물 iAHRS 로 확인. **IMU 는 포켓의 −X 끝(터널 반대쪽)에 붙인다** — 본체와 같은 자세 |
| `fit_pocket_lid.stl` | 75.6 × 51.6 × 3 | 2 (뚜껑) |
| `fit_joint_plug.stl` | 60 × 60 × 40 | 3 — 이음 여유 0.4 확인 |
| `fit_joint_socket.stl` | 40 × 40 × 42 | 3 (짝) |
| `fit_cradle_oak.stl` | 90.9 × 42 × 86 | 4 — OAK 미발주, 목업으로 간접 |
| `fit_oak_mockup.stl` | 81.9 × 31 × 116.9 | ❌ **뽑지 말 것** — 같은 프린터로 뽑으면 오차가 상쇄돼 시험이 무의미. 크레들 안쪽 폭을 캘리퍼로 재서 81.9×31 과 비교할 것 |

⚠️ 쿠폰만 고치지 말 것. 결과가 안 맞으면 `tools/cad/mast.py` 파라미터를 고치고 다시 생성한다.

## 2026-09-16 검토 반영 (v4.1)
1. **IMU 터널 개방** — `IMU_CABLE_SPACE` 14→24. 전엔 IMU 몸통(x 끝 10.5)이 터널 입구(x 6~14)를 56% 막았다. 포켓 50→60, 뚜껑 66→76. 베이스는 160×148×20 그대로.
2. **크레들 뒷판 접지** — 뒷판 밑을 바닥턱 밑면까지 내림. 전엔 D455 3.5 / OAK 3.95mm 떠서 서포트가 필요했다.
3. **이음 쿠폰에 칼라 추가** — 베이스↔마디는 플러그(안)+칼라(바깥) 이중 끼움인데 쿠폰이 안쪽만 시험했다. 프린트 오차가 겹치는 **칼라 쪽이 더 빡빡하다**.

남긴 것: 뚜껑 나사 보스 살두께 2.75mm. 나일론 M3 셀프탭에 갈라질 수 있으나 **이건 쿠폰이 답을 줄 항목**이라 그대로 뽑아본다. 갈라지면 `LID_MARGIN` 8→12.

## 2026-09-16 추가 수정 (v4.2)
4. **쿠폰 케이블 터널이 관통이 아니었다** — 포켓 벽을 1mm 파고 막힌 맹공이라 케이블을 꿰어볼 수 없었다.
   (본체 `mast.py` 는 중앙 Ø22 구멍까지 정상 관통. 쿠폰 쪽만 틀렸다.) 블록 밖까지 뚫도록 수정.

**프린트 전 메시 검사 통과** (5종 전부): 비정상 모서리 0, winding 오류 0, 닫힌 solid.
